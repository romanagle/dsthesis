# Project Name

## Goal
One paragraph: what question + what data + what output.

## Repo structure
- `src/`: reusable code
- `scripts/`: runnable entrypoints
- `configs/`: experiment configs
- `notebooks/`: exploration
- `data/`: local data (not tracked)
- `results/`: figures/tables (tracked), runs (not tracked)

## Setup
### Option A: conda
```bash
conda env create -f environment.yml
conda activate project
```
